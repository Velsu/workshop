.review
  %p.name
    = review.author
    %b= review.rating
  %p.review_content= review.content
  -if is_admin?
    = link_to "Delete", category_product_review_path(product.category, product, review), method: :delete, data: { confirm: "Are You Sure?" }